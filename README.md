# tk-mandelbrot-set
Python3 real-time Julia sets generator in tkinter. 
Run `main.py`. Click to change the constant *c* in the function z → z<sup>2</sup>+c and you will get the border of this function's prisoner set - the Julia set.
Press <kbd>ctrl</kbd>+<kbd>S</kbd> to export the prisoner set as a bitmap (PNG). There is the Mandelbrot set in the background as a "map".

Requirements:
```
sortedcontainers
PIL
```
![Running example GIF](/images/example.gif)

